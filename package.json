{
  "name": "smbo",
  "version": "0.0.1",
  "type": "module",
  "sideEffects": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "fmt": "pnpm biome check . && pnpm lint:ws && pnpm typecheck",
    "sso": "aws sso login --sso-session=smbo --no-browser"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.3",
    "@types/bun": "latest",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "sst": "3.1.64"
  }
}
